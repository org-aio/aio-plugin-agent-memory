# 记忆工作台设计记录

这是一份可删除的演示资料。

[[图谱记忆]] 用节点记录知识，用有方向的关系连接概念与来源。
[[Compose]] 负责本地的组件状态、节点选择和拖动；[[PostgreSQL]] 保存正文、标签和关系。
[[AIO]] 提供隔离运行和租户上下文。插件的前端与后端共用同一个发布版本。
[[双向链接]] 保留文档提及的概念，[[上下文检索]] 为 LLM 返回带来源 ID 的资料。

决定：保留来源正文；不把自动推测记为已证实事实；删除节点时一并删除关联边。
