# 随心记

`Notes` 编排来源列表、服务端搜索和分页；`composer` 使用稳定请求 ID 收件，按空间保留草稿。`item` 只展示净化后的 Markdown，`dialogs` 提供完整详情、显式打开原文编辑和确认删除。

编辑发出 `PUT /sources/{id}` 并携带预览版本；失败不关闭编辑器。原文只在提交者主动点击编辑后获取，不写浏览器持久缓存。来源权限由后端返回，服务端继续重新鉴权。
