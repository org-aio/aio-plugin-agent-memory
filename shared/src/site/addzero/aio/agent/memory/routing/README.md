# 对话分流

纯 Kotlin 规则只接收隔离后的文本。明确保存与查找走本地处理，复合任务与不确定输入交给模型；规则不执行内容中的指令，不解密秘密。

完整匹配的简短问候（如 `hi`、`你好`）走本地回复，不检索、不调用模型。聊天入口仍加密保存原文并保留净化对话来源，状态为 `recorded`，不建整理任务，也不进入搜索和图谱。带事实、保存指令或秘密引用的混合输入不按问候处理；笔记和文件导入不适用聊天跳过规则。
