# 上下文检索

受限邻域展开与带节点 ID、来源和关系的 Markdown 输出。不把检索资料当指令。

## 对话分类与激活

`ChatRouting.kt` 读取已隔离来源，明确查找直接返回净化摘录，复杂请求构造至多 6000 字符的模型上下文。来源本身排除召回，直接命中与实际上下文节点分别返回；图谱激活请求优先包含邻域并重新校验空间可见性。

完整问候返回 `greeting` 本地回复和空引用、空激活列表，不触发检索或模型请求。

本地回复将秘密占位引用显示为“保密字段”，保留来源引用供受控查看；模型上下文仍保留可校验的字段引用。
